这份文档为你（或你的Android开发人员）以及服务器端开发人员提供了清晰的开发标准。

既然是局域网控制，最稳定且简介的架构是：让 Android APP (NetControl) 在后台运行一个轻量级的本地 HTTP 服务（例如使用 NanoHTTPD 框架），这样服务器端就可以像调用普通 API 一样，直接给局域网内的设备发送指令。

以下是完整的开发文档与接口说明，你可以直接发给你的团队或服务端开发人员。

“NetControl” APP 开发与接口协议文档
一、 项目概述

NetControl 是一款基于 Android 原生开发的轻量级局域网网页展示终端。

核心功能：开机/打开APP后全屏无边框展示指定网页，自动适应各种屏幕分辨率（如 480x480, 1920x1080 等）。

控制方式：APP 内部运行本地 HTTP 监听服务，接收局域网内中控主机的查询与控制指令。

状态记忆：断电或重启后，自动恢复上一次接收到的网页 URL 并全屏显示。

二、 通信架构设计

通信协议：基于 HTTP + JSON，局域网内直连通信。

监听端口：Android APP 固定监听局域网内的 8080 端口（可自定义）。

角色分配：

NetControl (Android端)：作为轻量级服务端（HTTP Listener），被动接收请求，执行屏幕刷新。

局域网主机 (Server端)：作为客户端，主动发起设备扫描查询，主动下发 URL 变更指令。

三、 接口协议说明（提供给服务端开发）

服务端通过向 Android 设备的 IP 地址和特定端口（如 http://<设备IP>:8080）发送 HTTP 请求来进行交互。

1. 设备在线查询接口 (Device Status)

功能：服务端扫描局域网 IP 段，向该接口发请求，如果返回成功，说明该 IP 运行着 NetControl，并获取当前设备的运行状态。

请求方式：GET

请求路径：http://<设备IP>:8080/api/status

返回格式：application/json

返回示例 (200 OK)：

code
JSON
download
content_copy
expand_less
{
  "code": 200,
  "msg": "success",
  "data": {
    "device_name": "NetControl_Terminal",
    "device_ip": "192.168.1.105",
    "current_url": "http://192.168.1.100/display1.html",
    "screen_resolution": "1920x1080",
    "is_fullscreen": true
  }
}
2. 下发网页控制指令接口 (Set URL)

功能：服务端向指定设备发送控制指令，要求设备立即全屏加载新的 URL。

请求方式：POST

请求路径：http://<设备IP>:8080/api/set_url

Content-Type：application/json

请求参数 (Body)：

code
JSON
download
content_copy
expand_less
{
  "url": "http://192.168.1.100/new_dashboard.html"
}

注：
- url 支持 http://, https://，或者本地测试用的空页面。
- url 会做首尾空格/引号/反引号清理（建议请求侧不要额外包裹反引号）。

可选参数（用于需要鉴权的页面）：

code
JSON
download
content_copy
expand_less
{
  "url": "http://192.168.18.119:1880/static/",
  "basic_user": "admin",
  "basic_pass": "cre@admin"
}

说明：
- basic_user/basic_pass：用于 HTTP Basic Auth（弹窗账号密码）或需要预置 Authorization 的场景。
- headers：可选自定义请求头（仅对首次主文档请求生效），示例：

code
JSON
download
content_copy
expand_less
{
  "url": "http://192.168.1.100/new_dashboard.html",
  "headers": {
    "X-Auth-Token": "xxxx",
    "Cookie": "a=b; c=d"
  }
}

返回示例 (200 OK)：

code
JSON
download
content_copy
expand_less
{
  "code": 200,
  "msg": "Command received, loading URL...",
  "data": null
}
四、 Android 端开发说明（提供给 APP 开发）
1. UI 与分辨率自适应

布局文件 (XML)：不要写死任何尺寸。整个 Activity 只放置一个 android.webkit.WebView，其 layout_width 和 layout_height 均设置为 match_parent。这样无论是 480x480 的正方形屏，还是 1080p 的长屏，WebView 都会自动铺满。

沉浸式全屏 (无地址栏)：在 Activity 的 onCreate 中隐藏状态栏、导航栏，使用 SYSTEM_UI_FLAG_IMMERSIVE_STICKY 标志位，确保用户滑动边缘也不会退出全屏。

2. WebView 核心配置

为了确保网页完美显示，必须开启以下 WebView 属性：

code
Java
download
content_copy
expand_less
WebSettings settings = webView.getSettings();
settings.setJavaScriptEnabled(true); // 允许执行 JS
settings.setDomStorageEnabled(true); // 允许网页本地存储 (很重要)
settings.setUseWideViewPort(true); // 适应屏幕
settings.setLoadWithOverviewMode(true);
// 针对 Android 9.0+ 允许 HTTP 明文请求（在 AndroidManifest.xml application 中添加 android:usesCleartextTraffic="true"）
3. 数据记忆功能 (本地持久化)

使用 Android 的 SharedPreferences 保存收到的 URL。

逻辑：

APP 刚启动时，读取 SharedPreferences 中的 last_url 键值。

如果有值，立即加载该 URL；如果没有值，加载一个默认的本地空白页或提示页（"等待主机下发指令..."）。

每次通过 API 收到新的 URL 时，除了让 WebView 加载，必须覆盖写入 SharedPreferences 中的 last_url。

4. 局域网监听服务实现 (重点)

由于原生 Android 没有内置简单的 HTTP Server，极度推荐使用 NanoHTTPD 第三方开源轻量级库。它只有几个 Java 文件，体积忽略不计，专门用于这种场景。

引入依赖 (build.gradle)：

code
Gradle
download
content_copy
expand_less
implementation 'org.nanohttpd:nanohttpd:2.3.1'

实现逻辑：
继承 NanoHTTPD 类创建一个服务，监听 8080 端口。在 serve() 方法中拦截 /api/status 和 /api/set_url 的请求。

UI 线程切换：
注意： HTTP 请求是在后台线程收到的，而操作 WebView 必须在 Android 的主线程（UI线程）进行。收到 /api/set_url 指令后，必须通过 runOnUiThread() 或者 Handler 通知 WebView 去 loadUrl(newUrl)。

5. 权限申请 (AndroidManifest.xml)

APP 需要的基础权限：

code
Xml
download
content_copy
expand_less
<!-- 允许访问网络 -->
<uses-permission android:name="android.permission.INTERNET" />
<!-- 允许获取局域网 IP 状态 -->
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
<!-- 允许开机自启动（如果设备需要无人值守，建议加上） -->
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
五、 系统工作流程图

开机/启动 ➔ NetControl APP 运行。

初始化 ➔ 隐藏所有系统 UI (全屏) ➔ 读取上次保存的 URL 并加载 ➔ 启动 8080 端口后台监听。

主机扫描 ➔ 服务端通过遍历 IP 发送 GET /api/status ➔ 发现 NetControl 设备并记录 IP。

下发指令 ➔ 服务端发送 POST /api/set_url (附带新网址)。

APP执行 ➔ 收到 JSON ➔ 保存新网址到本地 ➔ 切换到主线程刷新 WebView 全屏显示新网页 ➔ 返回成功确认给服务端。
